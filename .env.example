# Telegram is called directly from the client, so these are VITE_-prefixed (exposed to the
# build). NOTE: they end up in the public bundle — only use a private, disposable bot.
VITE_TELEGRAM_BOT_TOKEN=123456789:your-bot-token
VITE_TELEGRAM_CHAT_ID=your-chat-id
